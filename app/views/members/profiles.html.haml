- title ("Meet the Members")

%section#header
  %h1 Meet Members of the UCCN
    
%section.main
  %section.full_width
    - if @users.empty?
      .message.warning{:style => "display: block; "}
        %p No Profiles to Display
    - else   
      - @users.each do |u|
        .info
          =link_to u.name, show_member_profile_path(u.id)
          =image_tag u.photo.url(:thumb) unless !u.photo?